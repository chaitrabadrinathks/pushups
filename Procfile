web: gunicorn app:app

### we are trying to say its a web based application.. and saying use gunicorn server(which is ###WSGI for python to host application on server)... app:app ... first app is folder/file where ###flask app is..2nd is flask app which we created [basically in create_app() which returned app]
### instead of gunicorn we can also use flask run to setup